// At the top:
export default function MainContentService({
  openOrderModal,
  modalOpen,
  setModalOpen,
  orderDetails,
  handleWeightChange,
  confirmOrder,
}) {
  // Remove internal modal state and handlers

  // useEffect for fade-in stays the same

  // Use openOrderModal passed as prop

  // In modal JSX use modalOpen, orderDetails, setModalOpen, handleWeightChange, confirmOrder from props

  // The rest remains the same
}
